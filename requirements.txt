# ─── Serveur web ───────────────────────────────────────────────
flask>=3.0.0               # Serveur HTTP et routing
werkzeug>=3.0.0            # Utilitaires Flask (sécurité noms de fichiers, etc.)

# ─── QR Code ───────────────────────────────────────────────────
qrcode[pil]>=7.4.2         # Génération du QR code
Pillow>=10.0.0             # Requis par qrcode[pil] pour render image

# ─── Réseau local ──────────────────────────────────────────────
zeroconf>=0.131.0          # mDNS : découverte auto sur le réseau local
ifaddr>=0.2.0              # Détection de l'IP locale de façon fiable

# ─── Interface graphique ────────────────────────────────────────
# tkinter est inclus dans Python standard (pas besoin de l'installer)
# Si tu utilises PyQt6 à la place, décommente la ligne ci-dessous :
PyQt6>=6.6.0

# ─── Sécurité / Auth ───────────────────────────────────────────
bcrypt>=4.1.0              # Hashage du PIN si mode auth activé

# ─── Utilitaires ───────────────────────────────────────────────
humanize>=4.9.0            # Affiche "1.2 MB" au lieu de "1258291 bytes"
python-dotenv>=1.0.0       # Chargement optionnel de variables d'env
